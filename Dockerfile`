# 使用官方 Python 3.9 镜像作为基础镜像
FROM python:3.9-slim-buster

# 设置工作目录
WORKDIR /app

# 将当前目录的内容复制到容器的 /app 目录下
COPY . /app

# 安装你的脚本可能需要的任何依赖 (如果 main.py 有依赖的话)
# 例如：RUN pip install -r requirements.txt

# 设置容器启动时执行的命令
CMD ["python3", "Unfolding/main.py"]